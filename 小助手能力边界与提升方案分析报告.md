# xunke 小助手能力边界与提升方案分析报告

本文档基于当前项目真实代码、`AGENTS.md`、接口实现、SQL 模板、语义规划资源、问答链路和巡课业务逻辑整理。本文只描述能力边界与提升方案，不包含任何敏感配置。

## 1. 当前小助手定位

当前小助手是高校 AI 巡课与课堂预警场景的查询分析助手，主要服务教学管理人员、督导、巡课管理人员和业务大屏嵌入场景。

它擅长回答：

- 当前或今日课堂运行情况。
- AI 巡课核心指标汇总。
- 到课率、抬头率、前排满座率等指标解释。
- 重点关注课堂、活力较高课堂。
- 课堂 AI 预警汇总、风险类型、风险等级。
- 近 7 天预警趋势、教师风险画像、视频回看线索。
- 基于结构化数据生成简洁业务简报。

它不是自由 SQL 助手，也不是通用数据库智能体。当前设计核心是：规则分桶、白名单 SQL/业务函数、语义规划、LLM 总结。

## 2. 当前已具备能力

| 能力 | 能力说明 | 入口接口或相关函数 | 依赖数据或模板 | 稳定程度 |
|---|---|---|---|---|
| 本地帮助/寒暄 | 回答“你是谁”“能做什么”“支持哪些问题” | `route_assistant_query()`、`_lookup_local_assistant_answer()` | 本地固定文案 | 高 |
| 指标解释 | 解释到课率、抬头率、前排满座率、巡查覆盖率等 | `_lookup_metric_kb_answer()` | `METRIC_KNOWLEDGE_BASE` | 高 |
| 实时巡课简报 | 查询当前正在上课课堂的巡课核心指标 | `/api/assistant/patrol/realtime-brief`、`get_realtime_patrol_brief()` | `t_tias_course`、`t_tias_classroom`、课表库回退逻辑 | 中 |
| 今日巡课汇总 | 查询今日已结束课程巡课指标 | `/api/assistant/patrol/daily-brief`、`get_daily_patrol_brief()` | `t_tias_course`、`t_tias_classroom`、课表库 | 中 |
| 到课率/抬头率/前排满座率查询 | 作为巡课指标参与汇总、Top、重点课堂判断 | `get_*patrol_brief()`、语义模板 | 巡课表字段、内置模板 | 中 |
| 重点关注课堂 | 按阈值识别低到课、低前排、低抬头课堂 | `get_*patrol_brief()`、`tpl_patrol_focus_top` | 阈值配置、`builtin_templates.py` | 中 |
| 活力较高课堂 | 识别表现较好的课堂 | `get_*patrol_brief()`、`tpl_patrol_vitality_top` | 阈值配置、`builtin_templates.py` | 中 |
| 实时课堂预警 | 查询近实时预警占比、等级、类型、Top | `/api/assistant/warning/realtime`、`get_realtime_warning_brief()` | `t_warning_study_record`、`t_warning_teaching_record`、课程表 | 中 |
| 今日课堂预警 | 查询今日预警汇总、等级、类型、课堂/学院排行 | `/api/assistant/warning/daily`、`get_daily_warning_brief()` | 预警记录表、巡课课程表 | 中 |
| 预警处置闭环 | 查询待处理、挂起、闭环等处置状态 | `get_warning_handle_overview_brief()` | 预警记录表 handle 字段 | 中 |
| 预警趋势 | 查询近 7 天预警趋势和高频类型 | `get_warning_trend_top_brief()` | 预警记录表 | 中 |
| 策略效果 | 查询告警策略/规则触发效果 | `get_alarm_strategy_effect_brief()` | `t_patrol_alarm_event`、`t_patrol_indicator_type` | 中 |
| 教师风险画像 | 查询教师相关预警排行 | `get_teacher_risk_profile_brief()` | 预警记录表、课程教师字段 | 中 |
| 教室健康/设备运维/敏感词 | 提供专项简报 | `get_classroom_health_brief()`、`get_device_ops_brief()`、`get_sensitive_word_brief()` | 对应巡检/状态/敏感词表 | 中偏低 |
| 视频点位/预警回看 | 查询视频点位或最近预警对应视频 | `/api/assistant/video/points`、`get_video_points()`、`get_warning_video_trace_brief()` | 视频历史、媒体录像、预警记录 | 中偏低 |
| SQL 模板查询 | 执行白名单模板，做参数校验和审计 | `/api/query/execute`、`QueryService.execute()` | `ai_query_template_registry`、内置模板、`seed_query_templates.sql` | 高 |
| 语义规划 | 将问题映射成 DSL / QueryPlan | `/api/assistant/plan`、`plan_auto_query()` | `resources/assistant_semantic/*.json` | 中 |
| 语义模板执行 | 执行 QueryPlan 中的模板 | `execute_semantic_plan()` | QueryService、模板注册表/内置模板 | 中，是否在线启用待确认 |
| LLM 总结 | 将结构化数据组织成自然语言简报 | `_summarize()`、`compose_semantic_report()` | OpenAI-compatible `/chat/completions` | 中偏低，依赖 LLM 可用性 |
| 通用问答 | 非业务问题走通用 LLM | `_general_llm_answer()` | LLM 服务 | 低到中 |
| Playground demo | 提供模拟问答演示 | `/api/playground/ask`、`demo.py` | 本地随机/模拟数据 | 高，但非真实业务数据 |

## 3. 当前能力边界

| 问题类型 | 当前能否支持 | 原因 | 建议处理方式 |
| ---- | ------ | -- | ------ |
| 数据库中没有的数据 | 不能稳定支持 | 只能查当前库表和模板覆盖的数据 | 明确提示暂无数据或需接入数据源 |
| 没有 SQL 模板/业务函数覆盖的问题 | 不能稳定支持 | 禁止自由 SQL，语义规划也依赖模板映射 | 补模板或补业务函数 |
| 复杂跨表任意分析 | 低支持 | 当前语义规划是关键词/权重映射，不是完整 Text2SQL | 先收敛问题范围，分阶段补模板 |
| 实时视频识别/画面理解 | 不支持或待确认 | 代码只查视频记录/回看区间，未见视频 AI 推理链路 | 标记待确认，接入视频服务后再支持 |
| 修改业务数据/配置 | 不支持 | 当前是查询助手，且安全规则禁止写操作 | 拒绝或引导管理员后台操作 |
| 跨租户查询 | 不支持 | 查询依赖 `tenant_id` / `tenant_org_code` 隔离 | 拒绝跨租户，要求指定合法租户 |
| 预测未来教学风险 | 不能稳定支持 | 当前是历史/实时查询和 LLM 总结，无预测模型 | 只能说明当前趋势，预测能力待建设 |
| 解释 `.env`、密钥、连接串 | 不支持 | 敏感信息保护要求 | 拒绝输出敏感配置 |
| 绕过权限/鉴权 | 不支持 | 当前代码未确认鉴权层，不能绕过安全边界 | 交由平台权限体系确认 |
| “情况怎么样”这类模糊问题 | 低支持 | 缺时间、业务域、对象、指标 | 先追问统计口径 |
| 多对象对比但对象不明确 | 低支持 | 参数抽取只能识别部分学院/教师/课程/教室/节次 | 追问比较对象和指标 |
| 需要导出 Excel/PDF | 当前代码未确认支持 | 未见导出接口 | 作为新能力设计 |
| 大规模长周期统计 | 待确认 | 当前多为今日、近 2 小时、近 3/7/30 天口径 | 先确认性能和索引 |

## 4. 需要追问用户的情况

当前代码已有 LLM 澄清和规则澄清，重点判断 `domain`、`time_scope`、`entity_role`、`dimension`、`metric`。

推荐追问场景与话术：

- 未说明时间范围：  
  “你希望统计哪个时间范围？例如当前实时、今日已结束、近 7 天，还是近 30 天？”

- 未说明业务域：  
  “你想看 AI 巡课情况，还是课堂 AI 预警情况？”

- 未说明对象角色：  
  “你提到的对象是教师、课程、学院，还是教室？”

- 问“情况怎么样”但无指标：  
  “你想重点看到课率、抬头率、前排满座率、预警占比，还是整体简报？”

- 预警问题缺范围：  
  “你想看实时预警、今日预警汇总，还是近 7 天预警趋势？”

- 排行/Top 问题缺维度：  
  “你想按哪个维度排行？可以按学院、课程、教师、教室、节次或风险类型。”

- 多对象比较缺对象：  
  “请告诉我要比较的对象，例如两个学院、两门课程或两位教师。”

- 涉及导出、推送、配置修改等高风险操作：  
  “当前我可以先生成查询结果或推送预览；真正导出、推送或修改配置需要确认权限和操作入口。”

## 5. 当前问答链路分析

| 步骤 | 主要文件/函数 | 说明 | 风险点 |
|---|---|---|---|
| 前端输入 | `main.py` `/assistant` 内嵌 JS | 调 `/api/assistant/ask-stream`，带 `tenant_id`、`session_id` | 页面逻辑与后端混在大文件中 |
| API 接入 | `assistant_ask_stream()` / `assistant_ask()` | 返回 NDJSON 或普通 JSON | 流式中断时只能返回兜底文案 |
| 本地问答 | `_lookup_local_assistant_answer()` | 处理寒暄、帮助、身份介绍 | 覆盖面有限 |
| 指标释义 | `_lookup_metric_kb_answer()` | 解释本地指标知识库 | 指标字典不统一，后续易分叉 |
| 参数抽取 | `_extract_query_options()` | 提取 TopN、学院、教师、课程、教室、节次、本科/研究生 | 正则抽取，复杂自然语言容易漏 |
| 上下文 | `_assistant_context_get/set()` | 内存缓存上一轮业务条件 | 多进程不共享，重启丢失 |
| 规则分桶 | `simple_assistant_bucket()` | 关键词判断业务 bucket | 关键词冲突时可能误分桶 |
| 追问 | `_build_llm_clarification_response()` / `_build_clarification_response()` | 判断是否缺时间、领域、维度、实体角色 | LLM 超时或 JSON 格式错误时降级 |
| 业务查询 | `_try_simple_bucket_answer()`、`get_*_brief()` | 调固定业务函数查库 | SQL 和表结构变更会影响稳定性 |
| 语义规划 | `plan_auto_query()` | 生成 DSL/QueryPlan | 规则权重较简单，覆盖有限 |
| 模板执行 | `execute_semantic_plan()`、`QueryService.execute()` | 执行白名单模板 | 实际是否启用模板执行取决于配置，待确认 |
| LLM 总结 | `_summarize()`、`compose_semantic_report()` | 生成简报 | LLM 可用性、幻觉和格式稳定性风险 |
| 返回回答 | `assistant_ask_stream()` | NDJSON `meta/delta/done` | 前端只展示纯文本，缺结构化可视化 |

## 6. 当前主要限制

| 维度 | 当前现状 | 可能导致的问题 | 后续改进方向 |
|---|---|---|---|
| 数据覆盖 | 主要依赖巡课、预警、课表、视频/设备等表 | 数据缺失时只能兜底 | 建立数据覆盖清单和缺数提示 |
| SQL 模板覆盖 | 有传统 Txx 模板和内置 `tpl_*` 模板 | 未覆盖问题无法稳定回答 | 按高频问题补白名单模板 |
| 语义规划 | 基于 JSON 资源、关键词、权重打分 | 复杂意图和组合查询不稳 | 扩充意图、维度、指标和模板映射 |
| RAG/知识库 | 有 JSONL，但未确认代码加载使用 | 无法利用表字段知识增强回答 | 明确 RAG 接入方案后再接入 |
| LLM 稳定性 | 意图分类、澄清、总结、通用问答依赖 LLM | 超时、JSON 解析失败、回答漂移 | 加强 fallback、结构化校验和评测 |
| 多轮上下文 | 内存 TTL 缓存 | 多实例不共享，追问链路有限 | 引入可选持久会话或外部缓存，待确认 |
| 权限/租户隔离 | 查询注入 `tenant_id`，课表有 `tenant_org_code` | 外部鉴权层未确认 | 增加租户隔离测试和鉴权集成说明 |
| 指标口径统一 | 指标口径散落在本地 KB、模板、LLM prompt、文档 | 口径不一致 | 建统一指标字典 |
| 前端交互 | 内嵌页面、纯文本流式展示 | 不利于图表、表格、追问按钮 | 拆分前端或增强结构化组件 |
| 测试评测 | 未发现明确测试体系 | 改动难验证 | 建接口测试、SQL 模板测试、问答评测集 |
| 日志可观测性 | 有内存 trace，无统一 logging | 生产排障有限 | 增加结构化日志与敏感信息脱敏 |
| 性能并发 | PyMySQL 每次连接，无连接池；大文件单模块 | 并发和慢查询风险 | 连接池、查询缓存、慢查询监控、模块拆分 |

## 7. 如何提升小助手能力

### 7.1 短期提升，1 到 2 周内可做

- 补充高频问法关键词和别名，降低误分桶。
- 完善追问规则，尤其是“情况怎么样”“哪个最多”“某人/某课风险”。
- 增加指标口径说明，统一本地 KB 与语义资源中的指标名称。
- 增加常见问题模板，如“学院今日巡课排名”“低到课率课堂 Top5”。
- 完善错误兜底话术，区分无数据、库连接失败、LLM 失败、模板缺失。
- 增加更多白名单 SQL 模板，优先覆盖领导/督导高频问题。
- 整理 50 到 100 条问答评测问题集。
- 开启并规范 trace 字段，避免记录敏感内容。
- 为 `/api/assistant/plan` 和 `/api/query/execute` 写最小手工验证说明。

### 7.2 中期提升，1 到 2 个月可做

- 建立统一指标字典：指标名、别名、口径、来源表、默认时间范围。
- 建立 tool functions 分层，把 `patrol_api.py` 拆成巡课、预警、视频、设备、LLM、上下文模块。
- 完善 `resources/assistant_semantic/`，补维度、实体、模板参数 schema。
- 接入 RAG 知识库，但先确认 embedding、索引、召回、过滤和安全边界。
- 建立问答评测集和回归评测脚本。
- 增加自动化测试，覆盖参数抽取、分桶、模板执行、只读保护。
- 增加权限和租户隔离测试。
- 建立 SQL 模板 lint/check 机制，检查 `tenant_id`、逻辑删除、参数缺失。

### 7.3 长期提升，3 个月以上

- 建立可配置 Agent 编排：意图识别、工具选择、查询执行、报告生成分层可配置。
- 支持多轮任务型对话，如“先看学院，再展开某课程，再导出报告”。
- 支持图表、表格和结构化报表。
- 支持预警策略配置，但必须有权限和审计。
- 支持主动推送，如每日/每周巡课简报、企业微信/校内消息。
- 接入可观测性平台，追踪 LLM、DB、SQL、用户问题质量。
- 支持更复杂教学质量分析，如趋势归因、课程对比、学院画像。

## 8. 推荐优先级

| 优先级 | 改进项 | 价值 | 实现难度 | 涉及文件/模块 | 风险 |
|---|---|---|---|---|---|
| P0 | 明确敏感信息脱敏和 trace 安全边界 | 防止泄露 | 低 | `assistant_trace.py`、`patrol_api.py` | 低 |
| P0 | 建租户隔离/逻辑删除 SQL 检查清单 | 防跨租户/脏数据 | 中 | SQL 模板、`builtin_templates.py`、`patrol_api.py` | 中 |
| P0 | 整理核心接口手工验证集 | 提升变更安全 | 低 | 文档/测试脚本待定 | 低 |
| P1 | 补高频问题模板和语义资源 | 提升命中率 | 中 | `resources/assistant_semantic/`、`builtin_templates.py` | 中 |
| P1 | 完善追问逻辑和话术 | 降低误答 | 中 | `patrol_api.py` | 中 |
| P1 | 建统一指标字典 | 统一口径 | 中 | 语义资源、文档、模板 | 中 |
| P1 | 增强无数据/失败兜底 | 提升体验 | 低 | `patrol_api.py`、`semantic_executor.py` | 低 |
| P2 | 拆分 `patrol_api.py` | 降维护成本 | 中高 | 新业务模块 | 中 |
| P2 | 接入 RAG 检索 | 扩展知识覆盖 | 高 | `rag/`、新检索模块、LLM prompt | 高 |
| P2 | 自动化测试和问答评测 | 稳定迭代 | 中 | tests 待新增 | 中 |
| P3 | 图表/报表输出 | 提升产品表达 | 高 | 前端、接口结构 | 中高 |
| P3 | 主动推送和策略配置 | 扩展业务闭环 | 高 | 权限、审计、消息平台 | 高 |

## 9. 建议新增的能力清单

| 能力 | 用户问法示例 | 需要的数据 | 需要的 SQL / 工具函数 | 难度 | 是否适合当前版本 |
|---|---|---|---|---|---|
| 领导视角总结 | “今天全校课堂运行总体怎么样？” | 巡课、预警、课表、处置 | 全局汇总模板、报告函数 | 中 | 适合 |
| 督导异常发现 | “今天最需要督导关注的课堂有哪些？” | 巡课指标、预警记录 | 重点课堂 Top、预警 Top | 中 | 适合 |
| 课程表现对比 | “高等数学和大学英语哪个课堂状态更好？” | 课程维度巡课指标 | 课程过滤 + 对比模板 | 中 | 适合但需模板 |
| 学院课堂质量排名 | “今日学院课堂质量排名” | 学院聚合指标 | 学院排行模板 | 中 | 适合 |
| 教师课堂趋势分析 | “张老师近 7 天风险趋势” | 教师、课程、预警、巡课 | 教师过滤、趋势模板 | 中高 | 适合中期 |
| 低到课率原因辅助分析 | “这些课为什么到课率低？” | 到课率、课程属性、时间、历史 | 多维归因模板 | 高 | 中期 |
| 预警课堂自动归因 | “今天预警主要原因是什么？” | 预警类型、等级、课程/学院 | 类型分布 + Top + LLM 报告 | 中 | 适合 |
| 每日巡课简报 | “生成今日巡课日报” | 今日巡课/预警/处置 | 日报模板、报告函数 | 中 | 适合 |
| 每周教学运行分析 | “生成本周教学运行报告” | 7 天趋势、学院、课程、预警 | 周报模板 | 中高 | 中期 |
| 自然语言生成图表 | “画一下近 7 天预警趋势” | 趋势数据 | 结构化接口 + 前端图表 | 高 | 中期 |
| 预警消息智能推送 | “把高风险课堂推给负责人” | 预警、人员、消息渠道 | 推送工具、权限审计 | 高 | 不建议当前直接做 |
| 企业微信/微信/校内消息推送 | “发给辅导员” | 用户体系、渠道 API | 消息平台工具函数 | 高 | 中长期，需权限确认 |

## 10. 小助手回答边界建议

可放到产品页面或帮助文档：

> 我是舟小智 AI 小助手，可以帮你查询 AI 巡课、课堂预警、重点关注课堂、活力课堂、预警趋势、教师风险画像、视频回看线索等课堂运行数据，并生成简洁分析简报。  
> 你可以问：“今日 AI 巡课汇总”“当前课堂预警情况”“到课率最低的课堂有哪些”“近 7 天预警趋势”“到课率是什么意思”。  
> 如果你的问题缺少时间范围、对象或指标，我会先追问确认，避免按错误口径回答。  
> 我不能修改业务数据、绕过权限、跨租户查询、输出系统密钥或数据库连接信息，也不能回答数据库中没有或尚未接入的数据。对需要导出、推送、配置调整等操作，我会先给出查询或预览，实际执行需要确认权限和操作入口。

## 11. 后续开发建议

最推荐先做的 5 件事：

1. 整理高频问题评测集，覆盖巡课、预警、Top、趋势、指标释义、追问。
2. 补全语义资源和白名单模板，优先领导/督导高频问题。
3. 建立统一指标字典，统一代码、模板、文档、LLM prompt 口径。
4. 强化追问逻辑，减少模糊问题误答。
5. 建立 SQL 安全检查清单，强制检查租户隔离和逻辑删除。

不建议现在做的 5 件事：

1. 不建议直接接入自由 Text2SQL。
2. 不建议立即做真实推送/配置修改，权限和审计未确认。
3. 不建议把 `rag/` 私自接入生产链路。
4. 不建议大范围重构 `patrol_api.py` 后再验证，应先补评测。
5. 不建议引入大型依赖或新服务，除非明确收益和部署条件。

需要业务人员确认：

- 指标口径：到课率、抬头率、前排满座率、课堂活动指数、重点关注、活力课堂。
- 预警等级含义和处置建议边界。
- 领导/督导最常问的问题清单。
- 日报/周报内容结构。
- 推送策略和责任人规则。

需要后端开发确认：

- 生产环境是否有网关/鉴权。
- `assistant_auto_plan_template_execute_enabled` 实际是否开启。
- 是否允许拆分 `patrol_api.py`。
- 是否需要连接池、缓存、异步 DB。
- trace 是否可在生产开启及脱敏规则。

需要 DBA 或数据人员确认：

- 表字段含义、索引、逻辑删除字段。
- `tenant_id` 与 `tenant_org_code` 映射关系。
- SQL 模板性能和慢查询风险。
- RAG JSONL 是否与真实 schema 同步。
- 历史数据覆盖范围和同步延迟。

## 12. 待确认问题

- `rag/jy_application_digital_patrol_rag.jsonl` 是否已在外部链路使用。
- 生产环境实际 `.env` 开关值，尤其是语义模板执行、trace、只读模式；本文档不读取 `.env`。
- 是否存在外部鉴权、网关、统一用户体系。
- `offline_bundle/` 是否需要同步维护。
- 自动化测试体系是否在其他目录或外部仓库。
- 业务库字段 `delete_flag`、`recycle_sign` 等逻辑删除字段在所有表中的真实约定。
- 课表库 `tenant_org_code` 与业务库 `tenant_id` 的映射规则。
- 视频点位和预警回看是否只是查记录，还是还有外部视频 AI 服务。
- 企业微信/微信/校内消息推送是否已有外部平台接口。
- 生产部署是单实例还是多实例；若多实例，内存上下文和缓存会不一致。
- 当前内置模板和控制表模板是否完全一致。
- 真实 LLM 服务的稳定性、超时、并发能力和返回格式约束。
