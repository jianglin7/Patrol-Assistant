# 小助手能力提升任务拆解清单

本文档基于当前 `xunke` 小助手项目真实代码、`AGENTS.md`、接口实现、SQL 模板、语义规划资源、问答链路和巡课业务逻辑整理，用于后续开发排期、评审和验收。

## 1. P0：稳定性与安全优先任务

### 1.1 敏感信息与 Trace 脱敏检查

- 任务名称：敏感信息与 Trace 脱敏检查
- 背景问题：当前项目存在 `assistant_trace.py` 和 debug trace 接口，但未发现统一 logging 配置；trace 和异常信息如果处理不当，可能暴露 `.env`、API Key、数据库密码、完整连接串等敏感信息。
- 目标效果：开启 trace 或接口异常时，不输出敏感配置；错误信息保留排障价值，但不泄露密钥、token、数据库密码、完整连接串。
- 涉及文件或模块：
  - `query_service/app/assistant_trace.py`
  - `query_service/app/main.py`
  - `query_service/app/patrol_api.py`
  - `query_service/app/semantic_report.py`
- 具体修改点：
  - 梳理 trace 中记录的字段。
  - 限制异常文本长度。
  - 明确禁止记录 `.env`、API Key、数据库密码、完整连接串。
  - 对 LLM、数据库、外部接口异常做分类记录。
- 验收标准：
  - 开启 trace 后，debug trace 页面和接口不展示敏感信息。
  - LLM 失败、数据库失败、模板失败时，错误提示不含密钥或连接串。
  - 用户侧错误信息友好，不展示内部堆栈。
- 风险点：过度脱敏可能影响排障。
- 是否需要业务确认：否。

### 1.2 SQL 租户隔离与逻辑删除检查

- 任务名称：SQL 租户隔离与逻辑删除检查
- 背景问题：SQL 分散在 `patrol_api.py`、`builtin_templates.py`、`seed_query_templates.sql`；项目要求查询必须考虑 `tenant_id`、`tenant_org_code` 和逻辑删除字段。
- 目标效果：降低跨租户查询、已删除数据进入统计、统计口径不一致的风险。
- 涉及文件或模块：
  - `query_service/app/patrol_api.py`
  - `query_service/app/builtin_templates.py`
  - `query_service/sql/seed_query_templates.sql`
  - `query_service/app/service.py`
  - `query_service/app/db.py`
- 具体修改点：
  - 建立 SQL 审核清单。
  - 检查每条业务 SQL 是否包含租户隔离条件。
  - 检查是否处理 `delete_flag`、`recycle_sign` 等逻辑删除字段。
  - 检查时间范围、JOIN 关系、参数化方式。
- 验收标准：
  - 新增或修改 SQL 时，能说明涉及表、关联关系、过滤条件、租户条件、逻辑删除条件和影响范围。
  - 不新增自由 SQL 执行接口。
  - 不执行数据库写操作。
- 风险点：不同表逻辑删除字段可能不一致，需要 DBA 或数据人员确认。
- 是否需要业务确认：需要 DBA/数据确认。

### 1.3 LLM 失败降级兜底统一

- 任务名称：LLM 失败降级兜底统一
- 背景问题：当前 LLM 参与意图分类、澄清、业务总结、语义报告和通用问答；任一环节失败都可能影响回答体验。
- 目标效果：LLM 不可用时，业务查询能力仍可用；用户能获得清晰、可操作的失败提示。
- 涉及文件或模块：
  - `query_service/app/patrol_api.py`
  - `query_service/app/semantic_report.py`
  - `query_service/app/config.py`
- 具体修改点：
  - 梳理 LLM 调用失败路径。
  - 区分超时、连接失败、JSON 解析失败、空回答。
  - 统一兜底话术。
  - 确保业务简报有 fallback。
- 验收标准：
  - 模拟 LLM 不可用时，巡课/预警查询仍返回业务兜底。
  - 通用问答返回明确提示，而不是接口异常。
  - 语义报告失败时回退结构化简报。
- 风险点：兜底话术过泛会降低用户信任。
- 是否需要业务确认：需要确认话术。

### 1.4 最小接口验证清单

- 任务名称：最小接口验证清单
- 背景问题：当前未发现明确的自动化测试体系，功能改动后缺少统一回归标准。
- 目标效果：形成一组可重复执行的核心接口验证项，支撑后续迭代。
- 涉及文件或模块：
  - `query_service/app/main.py`
  - `query_service/app/patrol_api.py`
  - `query_service/app/service.py`
  - 后续测试文档或测试脚本：待确认。
- 具体修改点：
  - 整理健康检查验证。
  - 整理 assistant 普通问答和流式问答验证。
  - 整理 `/api/assistant/plan` 验证。
  - 整理 `/api/query/execute` 模板查询验证。
  - 整理 trace 开关下的 debug 验证。
- 验收标准：
  - 开发者可按清单完成核心链路验证。
  - 每条验证项标明依赖数据库、LLM 或纯本地。
  - 不包含数据库写操作。
- 风险点：部分验证依赖现场数据库和 LLM 服务。
- 是否需要业务确认：否。

## 2. P1：用户体验提升任务

### 2.1 完善追问逻辑

- 任务名称：完善追问逻辑
- 背景问题：用户常问“情况怎么样”“哪个最多”“某老师风险怎么样”等模糊问题，当前可能误分桶或按默认口径回答。
- 目标效果：缺少时间、业务域、对象、指标、维度时，先追问而不是硬答。
- 涉及文件或模块：
  - `query_service/app/patrol_api.py`
  - `_build_llm_clarification_response()`
  - `_build_clarification_response()`
  - `_extract_query_options()`
- 具体修改点：
  - 增加模糊问句识别。
  - 增加“业务域不清”“时间不清”“对象角色不清”“排行维度不清”的规则。
  - 优化推荐回复。
- 验收标准：
  - “高等数学这门课情况怎么样”能追问 AI 巡课还是课堂预警。
  - “哪个最多”能追问排行维度和时间范围。
  - “周霄一周内最多的风险类型”能追问对象是教师还是课程。
- 风险点：追问过多会影响效率。
- 是否需要业务确认：需要。

### 2.2 统一回答结构

- 任务名称：统一回答结构
- 背景问题：当前回答由业务 fallback、LLM 总结、语义报告等多个路径生成，结构可能不一致。
- 目标效果：核心业务回答统一包含结论、关键数据、建议、口径说明。
- 涉及文件或模块：
  - `query_service/app/patrol_api.py`
  - `query_service/app/semantic_report.py`
- 具体修改点：
  - 统一巡课简报格式。
  - 统一预警简报格式。
  - 统一无数据提示。
  - 统一 LLM prompt 输出要求。
- 验收标准：
  - 高频问题回答结构稳定。
  - 回答中不出现 SQL、模板 ID、数据库字段名等技术细节。
  - 无数据时明确说明“当前范围暂无匹配记录”。
- 风险点：过度模板化可能让回答显得僵硬。
- 是否需要业务确认：需要。

### 2.3 扩充指标解释

- 任务名称：扩充指标解释
- 背景问题：当前已有 `METRIC_KNOWLEDGE_BASE` 和 `metrics.json`，但指标解释、别名、来源和适用范围仍可补充。
- 目标效果：用户问指标含义、算法口径、适用场景时稳定回答。
- 涉及文件或模块：
  - `query_service/app/patrol_api.py`
  - `query_service/resources/assistant_semantic/metrics.json`
- 具体修改点：
  - 补充指标别名。
  - 补充指标口径说明。
  - 补充“暂无数据或为 0”的解释。
  - 对预警等级增加清晰解释。
- 验收标准：
  - 到课率、抬头率、前排满座率、课堂活动指数、AI 巡查覆盖率、预警占比、风险等级均能稳定解释。
- 风险点：指标口径与业务真实口径不一致。
- 是否需要业务确认：需要。

### 2.4 优化推荐问题

- 任务名称：优化推荐问题
- 背景问题：页面推荐问题应优先引导用户提问系统稳定支持的能力。
- 目标效果：用户点击推荐问题后，尽量命中真实业务查询链路。
- 涉及文件或模块：
  - `query_service/app/main.py`
  - `query_service/app/patrol_api.py`
- 具体修改点：
  - 按领导、督导、学院、运维场景整理推荐问题。
  - 避免推荐当前不能稳定回答的问题。
  - 与 `ASSISTANT_SUGGESTED_QUESTIONS` 保持一致。
- 验收标准：
  - 推荐问题能命中 `simple_assistant_bucket()` 或语义规划。
  - 推荐问题覆盖巡课、预警、趋势、教师风险、指标解释。
- 风险点：推荐项过多影响页面清爽度。
- 是否需要业务确认：需要。

### 2.5 多轮上下文边界增强

- 任务名称：多轮上下文边界增强
- 背景问题：当前上下文是内存缓存，支持“改成 Top3”“只看某学院”等追问，但跨业务域继承容易出错。
- 目标效果：简单追问更稳，跨业务域或重置条件时边界清晰。
- 涉及文件或模块：
  - `query_service/app/patrol_api.py`
  - `_assistant_context_get()`
  - `_assistant_context_set()`
  - `_merge_query_options()`
- 具体修改点：
  - 明确上下文继承规则。
  - 增加清空筛选条件的提示。
  - 防止从预警链路错误继承到巡课链路。
- 验收标准：
  - “改成 Top3”“只看计算机学院”“取消学院筛选”能正确处理。
  - 跨业务域追问不会错误复用旧条件。
- 风险点：多实例部署时内存上下文不共享。
- 是否需要业务确认：否。

### 2.6 错误兜底话术优化

- 任务名称：错误兜底话术优化
- 背景问题：数据库、LLM、模板、无数据等失败场景需要给用户不同提示。
- 目标效果：用户知道是“无数据”“系统繁忙”“问题不清楚”还是“暂不支持”。
- 涉及文件或模块：
  - `query_service/app/patrol_api.py`
  - `query_service/app/semantic_executor.py`
  - `query_service/resources/assistant_semantic/error_codes.json`
- 具体修改点：
  - 统一错误分类。
  - 增加建议下一步问法。
  - 避免输出内部异常堆栈。
- 验收标准：
  - 无数据、DB 连接失败、LLM 超时、模板缺失分别有可区分提示。
- 风险点：错误分类不准会误导用户。
- 是否需要业务确认：需要确认用户侧文案。

## 3. P2：能力扩展任务

### 3.1 新增高频 SQL 模板

- 任务名称：新增高频 SQL 模板
- 背景问题：当前模板覆盖实时/今日巡课、预警、趋势、教师风险等，但复杂排行、对比、归因仍有限。
- 目标效果：覆盖领导和督导高频问题。
- 涉及文件或模块：
  - `query_service/app/builtin_templates.py`
  - `query_service/sql/seed_query_templates.sql`
  - `query_service/resources/assistant_semantic/templates.json`
  - `query_service/app/service.py`
- 具体修改点：
  - 新增学院课堂质量排行模板。
  - 新增低到课率课堂 Top 模板。
  - 新增课程表现对比模板。
  - 新增教师近 7 天趋势模板。
  - 同步参数 schema 和 result schema。
- 验收标准：
  - `/api/assistant/plan` 能规划到新增模板。
  - 模板可通过 `QueryService` 执行。
  - SQL 包含租户隔离和逻辑删除条件。
- 风险点：SQL 性能、字段含义、业务口径。
- 是否需要业务确认：需要业务和 DBA。

### 3.2 建立业务 tool functions 分层

- 任务名称：建立业务 tool functions 分层
- 背景问题：`patrol_api.py` 文件过大，业务函数、LLM 调用、上下文、规则分桶混在一起。
- 目标效果：新增能力更容易复用和测试。
- 涉及文件或模块：
  - `query_service/app/patrol_api.py`
  - 后续新模块：待确认。
- 具体修改点：
  - 先梳理函数边界。
  - 按巡课、预警、视频、设备、LLM、上下文拆分职责。
  - 短期先做设计，不急于大改。
- 验收标准：
  - 形成拆分方案。
  - 新增能力不继续堆到单个超大函数区。
- 风险点：拆分容易引发回归。
- 是否需要业务确认：否。

### 3.3 完善语义规划资源

- 任务名称：完善语义规划资源
- 背景问题：当前 `resources/assistant_semantic/` 通过关键词和权重做规划，覆盖有限。
- 目标效果：自然语言命中率提升，减少误分桶。
- 涉及文件或模块：
  - `query_service/app/semantic_planner.py`
  - `query_service/resources/assistant_semantic/intents.json`
  - `query_service/resources/assistant_semantic/metrics.json`
  - `query_service/resources/assistant_semantic/templates.json`
- 具体修改点：
  - 增加意图别名。
  - 增加指标别名和权重。
  - 增加维度映射。
  - 增加时间范围映射。
  - 与新增模板同步。
- 验收标准：
  - 高频问题评测集中大部分问题能命中正确 bucket。
  - 语义规划输出 DSL / QueryPlan 可解释。
- 风险点：规则冲突导致误命中。
- 是否需要业务确认：需要。

### 3.4 RAG 接入预研

- 任务名称：RAG 接入预研
- 背景问题：当前看到 `rag/jy_application_digital_patrol_rag.jsonl`，但代码中未确认已加载使用。
- 目标效果：明确是否接入、如何接入、接入后解决什么问题。
- 涉及文件或模块：
  - `rag/`
  - 后续检索模块：待确认。
  - LLM prompt：待确认。
- 具体修改点：
  - 明确检索入口。
  - 明确索引构建方式。
  - 明确 embedding 模型。
  - 明确向量库或 ES。
  - 明确召回数量、过滤条件。
  - 明确检索结果如何进入 LLM。
- 验收标准：
  - 产出设计文档和离线 demo。
  - 不直接接入生产链路。
- 风险点：误把未验证的 RAG 接入生产，造成幻觉或错误 SQL。
- 是否需要业务确认：需要数据确认。

### 3.5 图表和结构化报表

- 任务名称：图表和结构化报表
- 背景问题：当前页面主要展示纯文本，趋势、排行、对比类问题更适合表格或图表。
- 目标效果：支持趋势图、排行表、日报结构化展示。
- 涉及文件或模块：
  - `query_service/app/main.py`
  - assistant 接口返回结构：待确认。
- 具体修改点：
  - 在不破坏 NDJSON 的前提下增加结构化数据展示能力。
  - 为 Top、趋势、对比类问题输出表格数据。
  - 前端渲染表格或图表。
- 验收标准：
  - Top 问题可展示表格。
  - 趋势问题可展示图表或结构化列表。
- 风险点：前端改动较大，接口兼容需谨慎。
- 是否需要业务确认：需要。

### 3.6 推送能力预览增强

- 任务名称：推送能力预览增强
- 背景问题：当前已有预警推送预览能力，但不应直接执行真实推送。
- 目标效果：先给出触发条件、推送对象、渠道、风险说明，不做真实发送。
- 涉及文件或模块：
  - `query_service/app/patrol_api.py`
  - 预警相关业务函数。
- 具体修改点：
  - 优化推送预览结构。
  - 标明“仅预览，未真实发送”。
  - 增加阈值说明和建议动作。
- 验收标准：
  - 用户问推送相关问题时，返回预览而非真实发送结果。
- 风险点：用户误解为已经发送。
- 是否需要业务确认：需要。

## 4. P3：架构演进任务

### 4.1 拆分 `patrol_api.py`

- 任务名称：拆分 `patrol_api.py`
- 背景问题：当前 `patrol_api.py` 承载规则分桶、查询条件抽取、上下文、巡课、预警、视频、设备、LLM、报告等多类逻辑。
- 目标效果：降低维护成本，减少单文件变更风险。
- 涉及文件或模块：
  - `query_service/app/patrol_api.py`
  - 后续新模块：待确认。
- 具体修改点：
  - 拆分 routing、filters、context、patrol、warning、ops、video、llm、report。
  - 保持对 `main.py` 的导入兼容。
- 验收标准：
  - 外部接口行为不变。
  - 高频问答回归通过。
- 风险点：回归面大。
- 是否需要业务确认：否。

### 4.2 抽象 Agent 编排层

- 任务名称：抽象 Agent 编排层
- 背景问题：当前问答链路主要写死在 `route_assistant_query()`。
- 目标效果：让意图理解、追问、取数、报告生成、返回结构可配置。
- 涉及文件或模块：
  - 新 agent/orchestrator 模块：待确认。
  - `query_service/app/patrol_api.py`
  - `query_service/app/semantic_*`
- 具体修改点：
  - 抽象步骤：理解问题、判断是否追问、选择工具、执行查询、生成报告。
  - 保留原有兜底逻辑。
- 验收标准：
  - 现有问答链路可用新编排复现。
  - 新能力可以通过配置接入。
- 风险点：架构复杂化。
- 是否需要业务确认：否。

### 4.3 建立统一指标字典

- 任务名称：建立统一指标字典
- 背景问题：指标口径散落在本地 KB、语义资源、SQL 模板、LLM prompt 和文档中。
- 目标效果：指标名、别名、口径、来源、展示规则统一维护。
- 涉及文件或模块：
  - `query_service/resources/assistant_semantic/`
  - `query_service/app/patrol_api.py`
  - `query_service/app/semantic_report.py`
- 具体修改点：
  - 建立指标字典结构。
  - 迁移本地指标解释。
  - 与 SQL 模板字段映射。
- 验收标准：
  - 指标解释和报告口径一致。
  - 新指标只需在一处维护。
- 风险点：需要统一历史口径。
- 是否需要业务确认：需要。

### 4.4 建立评测集与自动化测试

- 任务名称：建立评测集与自动化测试
- 背景问题：当前测试体系待确认，问答改动难回归。
- 目标效果：核心链路可重复验证。
- 涉及文件或模块：
  - 后续 `tests/` 或评测脚本：待确认。
  - `query_service/app/patrol_api.py`
  - `query_service/app/semantic_planner.py`
  - `query_service/app/service.py`
- 具体修改点：
  - 参数抽取测试。
  - 规则分桶测试。
  - 追问逻辑测试。
  - 语义规划测试。
  - 模板执行 mock 测试。
- 验收标准：
  - 本地可重复执行。
  - 覆盖 50 到 100 条高频问题。
- 风险点：需要构造稳定测试数据或 mock。
- 是否需要业务确认：需要提供真实问题样例。

### 4.5 日志与可观测性平台

- 任务名称：日志与可观测性平台
- 背景问题：当前主要是内存 trace，生产排障能力有限。
- 目标效果：定位 DB、LLM、SQL、规则分桶和用户问题质量问题。
- 涉及文件或模块：
  - `query_service/app/assistant_trace.py`
  - logging 配置：待确认。
  - 监控平台：待确认。
- 具体修改点：
  - 结构化日志。
  - trace id 贯穿。
  - 耗时统计。
  - 错误分类。
  - 敏感信息脱敏。
- 验收标准：
  - 能定位每次问答耗时瓶颈。
  - 能区分 LLM、DB、模板、解析错误。
- 风险点：日志量和敏感信息风险。
- 是否需要业务确认：否。

### 4.6 权限与租户校验增强

- 任务名称：权限与租户校验增强
- 背景问题：当前代码中未确认外部鉴权或网关，租户参数主要来自请求或默认配置。
- 目标效果：防越权访问和跨租户查询。
- 涉及文件或模块：
  - `query_service/app/main.py`
  - `query_service/app/service.py`
  - 外部鉴权/网关：待确认。
- 具体修改点：
  - 明确 tenant 来源。
  - 明确用户身份来源。
  - 明确接口权限。
  - 增加租户隔离测试。
- 验收标准：
  - 非授权租户无法查询。
  - 缺少 tenant 时行为明确。
- 风险点：需要外部系统配合。
- 是否需要业务确认：需要后端/平台确认。

## 5. 推荐实施顺序

| 顺序 | 任务 | 为什么先做 | 依赖项 | 验收方式 |
| -- | -- | ----- | --- | ---- |
| 1 | 敏感信息与 Trace 脱敏检查 | 先守住安全底线，避免调试功能泄露敏感信息 | 无 | 开启 trace 检查无密钥、连接串、数据库密码 |
| 2 | SQL 租户隔离与逻辑删除检查 | 防止跨租户和脏数据，是所有后续 SQL 能力的基础 | DBA 字段确认 | SQL 清单逐条通过 |
| 3 | 最小接口验证清单 | 后续改动必须有回归标准 | 可用测试环境 | 按清单完成核心接口验证 |
| 4 | 完善追问逻辑 | 直接解决“问不准、答不稳”问题 | 高频问题样例 | 模糊问题能正确追问 |
| 5 | 统一回答结构 | 提升回答可读性和可信度 | 业务话术确认 | 高频回答结构一致 |
| 6 | 扩充指标解释 | 降低用户理解成本，减少重复追问 | 指标口径确认 | 指标问答稳定返回 |
| 7 | 优化推荐问题 | 引导用户问系统擅长的问题 | 产品场景确认 | 推荐问题高命中 |
| 8 | 新增高频 SQL 模板 | 扩展真实查询能力 | SQL 安全清单 | 新问题可查真实数据 |
| 9 | 完善语义规划资源 | 提升自然语言命中率 | 新模板和评测集 | `/api/assistant/plan` 命中率提升 |
| 10 | 建立评测集与自动化测试 | 支撑长期迭代 | 样例数据或 Mock | 本地可重复回归 |

## 6. 第一阶段建议落地范围

第一阶段建议定位为“小助手稳定问答增强版”，适合 1 到 2 周内完成。

### 6.1 范围原则

1. 不贪大。
2. 优先解决用户问不准、答不稳、边界不清晰的问题。
3. 优先不大改架构。
4. 优先复用现有代码。
5. 不接入真实 RAG。
6. 不做真实推送。
7. 不新增大型依赖。
8. 不改部署结构。

### 6.2 建议纳入第一阶段的开发项

| 模块 | 落地内容 | 产出 |
| -- | -- | -- |
| 安全 | trace/error 脱敏检查；明确不输出敏感信息 | 安全检查清单和小范围代码防护 |
| SQL | 梳理现有核心 SQL 的租户隔离、逻辑删除、时间条件 | SQL 审核表 |
| 追问 | 增强时间、业务域、对象、维度、指标缺失时的追问 | 更稳定的澄清回答 |
| 回答结构 | 统一巡课/预警简报 fallback 结构 | 结论、关键数据、建议、口径说明 |
| 指标解释 | 补充核心指标解释和别名 | 指标释义更完整 |
| 推荐问题 | 调整页面推荐问题，优先推荐稳定能力 | 用户更容易命中可答问题 |
| 验证 | 建最小手工验证集：10 到 20 个问题 | 可直接回归测试 |

### 6.3 第一阶段建议验收问题集

- 今日 AI 巡课汇总
- 当前实时 AI 巡课简报
- 今日课堂 AI 预警汇总
- 近 7 天预警趋势
- 到课率是什么意思
- 哪个学院预警最多
- 高等数学这门课情况怎么样
- 张老师近 7 天风险类型
- 改成 Top3
- 只看计算机学院
- 预警推送阈值
- 最近一条预警的视频回看区间

### 6.4 第一阶段成功标准

1. 模糊问题不再轻易硬答，能追问。
2. 高频问题能命中正确业务 bucket。
3. 无数据、LLM 失败、DB 查询失败时有清晰兜底。
4. 回答中不出现 SQL、模板 ID、数据库字段名等技术细节。
5. 不暴露 `.env`、API Key、数据库密码、完整连接串。
6. 新增或调整的 SQL 均说明租户隔离和逻辑删除条件。
7. 不修改生产配置、不执行数据库写操作、不触碰离线包和大 SQL dump。
